# Enforce npm as the package manager
engine-strict=true

# Ensure package-lock.json is used and maintained
package-lock=true

# Use caret (^) for version ranges to maintain compatibility
save-prefix=^

# Security and performance
audit=true
fund=false

